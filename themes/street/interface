<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>VOLUME // ZERO // Curated Forms</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;700&family=Playfair+Display+SC&family=Plus+Jakarta+Sans:wght@200;600;800&display=swap" rel="stylesheet">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        volume: {
                            paper: '#F5F5F1',
                            charcoal: '#1C1C1C',
                            slate: '#777777',
                            rust: '#994D31' // Desaturated accent
                        }
                    },
                    fontFamily: {
                        sans: ['"Plus Jakarta Sans"', 'sans-serif'],
                        serif: ['"Cormorant Garamond"', 'serif']
                    }
                }
            }
        }
    </script>
    <style>
        body { background-color: #F5F5F1; color: #1C1C1C; font-family: 'Plus Jakarta Sans', sans-serif; overflow-x: hidden; }
        
        .fade-in { animation: volumeReveal 1s cubic-bezier(0.23, 1, 0.32, 1) forwards; }
        @keyframes volumeReveal { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }
        
        .hidden { display: none !important; }

        /* Spatial Interaction: Soft shadow expansion on hover */
        .spatial-hover {
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
            transform: translateY(0);
        }
        .spatial-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px -20px rgba(28, 28, 28, 0.1);
        }

        /* Fine lines at low opacity */
        .fine-line { border-bottom: 1px solid rgba(28, 28, 28, 0.05); }

        ::-webkit-scrollbar { width: 3px; }
        ::-webkit-scrollbar-thumb { background: #1C1C1C; }
    </style>
</head>
<body class="antialiased selection:bg-volume-charcoal selection:text-volume-paper">

    <nav class="fixed top-8 left-1/2 -translate-x-1/2 z-50 w-[90%] max-w-7xl">
        <div class="bg-volume-paper/80 backdrop-blur-lg border border-volume-charcoal/5 px-8 py-5 flex justify-between items-center shadow-sm">
            <a href="#home" class="font-extrabold text-sm uppercase tracking-[0.6em]">VOL//ZERO</a>
            
            <div class="flex items-center space-x-12">
                <ul class="hidden md:flex space-x-10 text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate">
                    <li><a href="#shop" class="hover:text-volume-charcoal transition-colors">Catalog</a></li>
                    <li><a href="#about" class="hover:text-volume-charcoal transition-colors">About</a></li>
                    <li><a href="#account" id="nav-account-link" class="hover:text-volume-charcoal transition-colors">Account</a></li>
                </ul>
                <a href="#cart" class="text-[10px] uppercase tracking-[0.2em] font-bold flex items-center gap-2 group">
                    Cart [<span class="js-cart-count">0</span>]
                </a>
                <button id="mobile-menu-btn" class="md:hidden">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 8h16M4 16h16"></path></svg>
                </button>
            </div>
        </div>
    </nav>

    <div id="mobile-menu" class="fixed inset-0 bg-volume-paper z-40 hidden flex flex-col p-12 space-y-8 text-2xl font-light">
        <button id="mobile-close-btn" class="self-end text-sm uppercase tracking-widest mb-12">Close</button>
        <a href="#shop" class="js-mobile-link fine-line pb-4">Catalog</a>
        <a href="#about" class="js-mobile-link fine-line pb-4">About</a>
        <a href="#account" class="js-mobile-link fine-line pb-4">Account</a>
        <a href="#cart" class="js-mobile-link fine-line pb-4">View Cart</a>
    </div>

    <main id="app-root" class="flex-grow pt-40 fade-in"></main>

    <footer class="mt-20 border-t border-volume-charcoal/5 px-10 py-24 bg-white/50">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-start gap-12 text-sm">
            <div class="space-y-4 max-w-sm">
                <p class="font-bold uppercase tracking-widest text-[11px]">Volume//Zero Studio</p>
                <p class="text-volume-slate leading-relaxed font-light">Curating essential forms for the contemporary environment. A dialogue between silhouette and space.</p>
            </div>
            <div class="flex flex-col space-y-3 opacity-60 text-[10px] uppercase tracking-widest font-semibold">
                <a href="#shop" class="hover:underline">Catalog</a>
                <a href="#about" class="hover:underline">About Studio</a>
                <a href="#" class="hover:underline">Instagram</a>
            </div>
            <p class="text-[9px] text-volume-slate uppercase tracking-widest self-end">© 2026 Volume Zero // G1-RSA</p>
        </div>
    </footer>

    <div id="toast-container" class="fixed bottom-10 right-10 z-[100] flex flex-col gap-3 pointer-events-none"></div>

    <template id="tpl-home">
        <div class="max-w-7xl mx-auto px-10 py-10">
            <header class="max-w-5xl mb-32">
                <h1 class="text-5xl md:text-8xl font-extralight tracking-tighter text-volume-charcoal mb-10 leading-[0.95]">
                    CURATED <br> <span class="font-extrabold text-volume-rust">ESSENTIALS</span> <br> FROM V//Z
                </h1>
                <p class="text-[11px] uppercase tracking-[0.5em] text-volume-slate font-medium max-w-md leading-relaxed">Collection 01 exploring the intersection of heavy bass culture and minimalist silhouettes.</p>
            </header>
            
            <div class="flex justify-center border-t border-volume-charcoal/5 pt-12">
                <a href="#shop" class="font-display font-black text-3xl uppercase tracking-tighter border-b-4 border-volume-charcoal pb-2 hover:border-volume-rust transition-all">
                    Explore Volume
                </a>
            </div>
        </div>
    </template>

    <template id="tpl-shop">
        <div class="max-w-7xl mx-auto px-10 py-12">
            <header class="flex justify-between items-end mb-24 gap-8">
                <h1 class="text-4xl md:text-5xl font-extrabold uppercase tracking-tighter js-collection-title">The Volume</h1>
                <div class="flex items-center gap-4 text-[10px] uppercase tracking-widest font-bold">
                    <span class="text-volume-slate">Showing</span>
                    <span class="js-collection-count text-volume-charcoal"></span>
                </div>
            </header>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-12 gap-y-24 js-product-grid"></div>
        </div>
    </template>
    
    <template id="tpl-product-card">
        <a href="#" class="group block spatial-hover js-prod-link">
            <div class="aspect-[4/5] bg-white p-12 mb-6 flex items-center justify-center text-[10px] uppercase tracking-widest text-volume-slate/30 js-img-placeholder">Image_Ref</div>
            <div class="px-3">
                <h3 class="text-xs uppercase tracking-widest font-bold text-volume-charcoal mb-1 js-title"></h3>
                <div class="flex justify-between items-center text-[10px] uppercase tracking-widest font-semibold text-volume-slate">
                    <span class="js-category"></span>
                    <span class="text-volume-rust js-price"></span>
                </div>
            </div>
        </a>
    </template>

    <template id="tpl-product">
        <section class="max-w-7xl mx-auto px-10 py-10 lg:py-20 flex flex-col md:flex-row gap-20">
            <div class="flex-1">
                <div class="aspect-[3/4] bg-white p-16 flex items-center justify-center text-xs tracking-widest text-volume-slate/20 shadow-inner js-detail-img">IMAGE_SOURCE</div>
            </div>
            <div class="flex-1 flex flex-col justify-center max-w-md">
                <span class="text-[10px] uppercase tracking-[0.3em] text-volume-slate mb-4 js-detail-cat"></span>
                <h1 class="text-4xl md:text-5xl font-extrabold uppercase tracking-tighter mb-4 js-detail-title"></h1>
                <p class="text-xl mb-12 font-medium js-detail-price"></p>
                
                <div class="space-y-6 mb-16">
                    <p class="text-[10px] uppercase tracking-widest font-bold text-volume-slate border-b border-volume-charcoal/5 pb-2">Description</p>
                    <p class="text-xs leading-relaxed font-light text-volume-charcoal/80 js-detail-desc"></p>
                </div>
                
                <div class="flex gap-4">
                    <button class="flex-1 bg-volume-charcoal text-volume-paper py-5 text-[11px] uppercase tracking-[0.4em] font-bold hover:bg-volume-rust transition-colors js-add-btn">
                        Acquire Form
                    </button>
                </div>
                
                <div class="mt-16 pt-8 border-t border-volume-charcoal/5 text-xs text-volume-slate/60 space-y-4 leading-relaxed font-light">
                    <p class="font-bold text-volume-charcoal">Note:</p>
                    <p>Each specimen is numbered. Handle with care. Clean according to internal data label.</p>
                </div>
            </div>
        </section>
    </template>

    <template id="tpl-cart">
        <section class="max-w-4xl mx-auto px-10 py-20 min-h-[60vh]">
            <h1 class="text-4xl font-extrabold uppercase tracking-tighter text-center mb-20 border-b border-volume-charcoal/10 pb-10">Inventory Queue</h1>
            <div class="js-cart-container"></div>
        </section>
    </template>

    <template id="tpl-cart-empty">
        <div class="text-center py-20 border border-dashed border-volume-charcoal/20">
            <p class="text-xs uppercase tracking-widest text-volume-slate mb-8">Queue is currently vacant.</p>
            <a href="#shop" class="font-display font-black text-2xl uppercase border-b-2 border-volume-charcoal pb-1">Browse CATALOG</a>
        </div>
    </template>

    <template id="tpl-cart-layout">
        <div class="space-y-12 js-cart-items mb-20"></div>
        <div class="border-t border-volume-charcoal/10 pt-12 flex flex-col items-end">
            <div class="flex justify-between w-full md:w-1/2 mb-12">
                <span class="text-[10px] uppercase tracking-widest text-volume-slate font-bold">Estimated Total</span>
                <span class="text-2xl font-bold js-cart-total text-volume-rust"></span>
            </div>
            <a href="#checkout" class="block text-center w-full bg-volume-charcoal text-volume-paper py-5 text-[11px] uppercase tracking-[0.3em] font-bold hover:bg-volume-rust transition-colors">Finalize Acquisition</a>
        </div>
    </template>

    <template id="tpl-cart-item">
        <div class="flex gap-10 items-center fine-line pb-8 group">
            <div class="w-24 h-28 bg-white flex-shrink-0 flex items-center justify-center text-[10px] text-volume-slate js-item-img">IMG_SOURCE</div>
            <div class="flex-grow flex flex-col md:flex-row md:items-center justify-between gap-4">
                <div>
                    <h3 class="font-black uppercase text-xl text-volume-charcoal js-item-title"></h3>
                    <span class="text-sm font-medium text-volume-rust js-item-price"></span>
                </div>
                <div class="flex items-center gap-6">
                    <div class="flex items-center space-x-4 border border-volume-charcoal/10 px-4 py-1">
                        <button class="js-qty-sub p-1">−</button>
                        <span class="js-item-qty w-4 text-center text-xs">1</span>
                        <button class="js-qty-add p-1">+</button>
                    </div>
                    <button class="text-sm text-red-400 hover:text-red-700 transition-colors js-item-remove">PURGE</button>
                </div>
            </div>
        </div>
    </template>

    <template id="tpl-account">
        <section class="max-w-md mx-auto px-8 py-24">
            <p class="text-[10px] uppercase tracking-[0.3em] text-volume-slate text-center mb-3 font-semibold">Account</p>
            <h2 class="font-extrabold text-3xl text-center mb-12 uppercase tracking-[0.2em] text-volume-charcoal">Welcome</h2>

            <div class="acc-login space-y-8">
                <form data-form="login" class="space-y-6">
                    <label class="block">
                        <span class="text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate block mb-2">Email</span>
                        <input type="email" name="email" required class="w-full bg-transparent border-b border-volume-charcoal/20 py-3 text-sm focus:outline-none focus:border-volume-charcoal transition-colors">
                    </label>
                    <label class="block">
                        <span class="text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate block mb-2">Password</span>
                        <input type="password" name="password" required class="w-full bg-transparent border-b border-volume-charcoal/20 py-3 text-sm focus:outline-none focus:border-volume-charcoal transition-colors">
                    </label>
                    <button type="submit" class="w-full bg-volume-charcoal text-volume-paper py-4 text-[10px] uppercase tracking-[0.3em] font-bold hover:opacity-90 transition-opacity">Sign In</button>
                    <p data-err="login" class="hidden text-[10px] uppercase tracking-widest text-red-700"></p>
                </form>
                <p class="text-[10px] uppercase tracking-[0.3em] text-volume-slate text-center font-semibold">
                    No account? <a href="#" data-action="show-register" class="text-volume-charcoal border-b border-volume-charcoal pb-0.5">Create one</a>
                </p>
            </div>

            <div class="acc-register hidden space-y-8">
                <form data-form="register" class="space-y-6">
                    <label class="block">
                        <span class="text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate block mb-2">Name</span>
                        <input type="text" name="name" class="w-full bg-transparent border-b border-volume-charcoal/20 py-3 text-sm focus:outline-none focus:border-volume-charcoal transition-colors">
                    </label>
                    <label class="block">
                        <span class="text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate block mb-2">Email</span>
                        <input type="email" name="email" required class="w-full bg-transparent border-b border-volume-charcoal/20 py-3 text-sm focus:outline-none focus:border-volume-charcoal transition-colors">
                    </label>
                    <label class="block">
                        <span class="text-[10px] uppercase tracking-[0.3em] font-semibold text-volume-slate block mb-2">Password (8+)</span>
                        <input type="password" name="password" required minlength="8" class="w-full bg-transparent border-b border-volume-charcoal/20 py-3 text-sm focus:outline-none focus:border-volume-charcoal transition-colors">
                    </label>
                    <button type="submit" class="w-full bg-volume-charcoal text-volume-paper py-4 text-[10px] uppercase tracking-[0.3em] font-bold hover:opacity-90 transition-opacity">Create Account</button>
                    <p data-err="register" class="hidden text-[10px] uppercase tracking-widest text-red-700"></p>
                </form>
                <p class="text-[10px] uppercase tracking-[0.3em] text-volume-slate text-center font-semibold">
                    Existing account? <a href="#" data-action="show-login" class="text-volume-charcoal border-b border-volume-charcoal pb-0.5">Sign in</a>
                </p>
            </div>

            <div class="acc-dashboard hidden space-y-10">
                <div class="space-y-2 text-center">
                    <p class="text-[10px] uppercase tracking-[0.3em] text-volume-slate font-semibold">Welcome,</p>
                    <h3 class="font-extrabold text-2xl uppercase tracking-[0.15em] text-volume-charcoal js-acc-name"></h3>
                    <p class="text-[10px] uppercase tracking-widest text-volume-slate js-acc-email"></p>
                </div>
                <div class="space-y-3">
                    <p class="text-[10px] uppercase tracking-[0.3em] text-volume-slate font-semibold">Order History</p>
                    <div class="js-orders-list space-y-2 border-t border-volume-charcoal/10 pt-4"></div>
                </div>
                <button type="button" class="js-logout-btn w-full border border-volume-charcoal py-4 text-[10px] uppercase tracking-[0.3em] font-bold hover:bg-volume-charcoal hover:text-volume-paper transition-colors">Sign Out</button>
            </div>
        </section>
    </template>

    <template id="tpl-checkout">
        <section class="max-w-2xl mx-auto px-10 py-20 min-h-[70vh]">
            <h1 class="text-3xl font-extrabold uppercase tracking-tighter mb-10 text-center">Acquisition System</h1>
            <form class="bg-white border border-volume-charcoal/5 p-12 space-y-10 js-checkout-form">
                <div>
                    <h2 class="text-sm font-bold text-volume-charcoal uppercase tracking-wider mb-8 border-b border-volume-charcoal/10 pb-2">Identify//Transmit</h2>
                    <div class="grid grid-cols-2 gap-4 font-light text-xs">
                        <input type="text" required placeholder="First given name" class="bg-volume-paper px-4 py-3 focus:outline-none focus:ring-1 focus:ring-volume-rust placeholder:text-volume-slate/50">
                        <input type="text" required placeholder="Surname" class="bg-volume-paper px-4 py-3 focus:outline-none focus:ring-1 focus:ring-volume-rust placeholder:text-volume-slate/50">
                        <input type="email" required placeholder="Transmit email" class="col-span-2 bg-volume-paper px-4 py-3 focus:outline-none focus:ring-1 focus:ring-volume-rust placeholder:text-volume-slate/50">
                        <input type="text" required placeholder="Street destination" class="col-span-2 bg-volume-paper px-4 py-3 focus:outline-none focus:ring-1 focus:ring-volume-rust placeholder:text-volume-slate/50">
                    </div>
                </div>
                <button type="submit" class="w-full bg-volume-charcoal text-volume-paper py-5 text-[11px] uppercase tracking-[0.3em] font-bold hover:bg-volume-rust transition-colors shadow-lg">Process Order System</button>
            </form>
        </section>
    </template>

    <template id="tpl-success">
        <div class="max-w-2xl mx-auto px-10 py-32 text-center min-h-[60vh] flex flex-col justify-center items-center">
            <div class="w-20 h-20 bg-volume-rust text-volume-paper flex items-center justify-center mx-auto mb-10 opacity-70">
                <svg class="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
            </div>
            <h1 class="text-5xl font-extrabold uppercase tracking-tighter mb-4 text-volume-charcoal">System Synced.</h1>
            <p class="text-volume-slate mb-12 text-sm max-w-sm">Order logged. Check transmit channel for confirmation.</p>
            <a href="#shop" class="bg-volume-charcoal text-volume-paper px-8 py-3 text-[11px] uppercase tracking-[0.3em] font-bold">Return to Void</a>
        </div>
    </template>

    <template id="tpl-about">
        <section class="max-w-3xl mx-auto px-10 py-20 text-center min-h-[60vh]">
            <h1 class="text-4xl md:text-6xl font-extrabold uppercase tracking-tighter mb-12 text-volume-charcoal">Manifesto.</h1>
            <p class="text-sm leading-loose text-volume-charcoal/70 js-static-content">Dedicated to formal validation and the persistence of architectural forms in high-weight environments. Every garment in the zero series is a technical iteration. Form is not separate from function, but the ultimate expression of it. Verified supply.</p>
        </section>
    </template>

    <script>
        // --- 1. MOCK DATABASE ---
        const DB = {
            currency: '$',
            categories: ['Attire', 'Apparatus', 'Storage'],
            products: [
                { id: 1, title: 'Modular Shell 01', price: 420, category: 'Apparatus', img: 'M_SHELL_G1', desc: '3-Layer technical nylon. Taped seams. Removable internal insulation module.' },
                { id: 2, title: 'Oversized Tee // Bone', price: 95, category: 'Attire', img: 'MW_TEE_BN', desc: 'Heavyweight organic cotton. Box fit. Reinforced collar.' },
                { id: 3, title: 'Cargo Artifact Pant', price: 210, category: 'Attire', img: 'STR_PNT_01', desc: 'Stretch technical canvas. Hidden cargo compartments. Adjustable cuff.' },
                { id: 4, title: 'Utility Tote System', price: 160, category: 'Storage', img: 'MCS_TOTE_U', desc: 'Ballistic nylon construction. Lasercut webbing for external expansion.' },
                { id: 5, title: 'Field Monitor Sconce', price: 310, category: 'Apparatus', img: 'LTG_FIELD_0', desc: 'Raw aluminum construction. LED array with warm glow.' }
            ]
        };

        // --- 2. CORE APPLICATION CLASS ---
        class StoreEngine {
            constructor() {
                this.root = document.getElementById('app-root');
                this.cart = JSON.parse(localStorage.getItem('vol_cart')) || [];
                this.auth = {
                    user: (() => { try { return JSON.parse(localStorage.getItem('vm_customer_cache') || 'null'); } catch { return null; } })(),
                    token: localStorage.getItem('vm_customer_token')
                };
                this.bootCustomer = this.bootCustomer.bind(this);
                setTimeout(() => this.bootCustomer(), 0);
                
                this.initLayout();
                this.initRouter();
                this.updateCartCount();
            }

            isLoggedIn() { return !!(this.auth && this.auth.user); }

            applyCustomerToDom() {
                document.body.dataset.customer = this.isLoggedIn() ? 'in' : 'out';
                const link = document.getElementById('nav-account-link');
                if (!link) return;
                if (this.isLoggedIn()) {
                    const name = (this.auth.user.name || this.auth.user.email || 'Account').toString();
                    link.textContent = name.length > 12 ? name.substring(0, 12) + '…' : name;
                } else {
                    link.textContent = 'Account';
                }
            }

            async bootCustomer() {
                this.applyCustomerToDom();
                if (!this.auth.token) {
                    document.dispatchEvent(new CustomEvent('vm:customer-logout'));
                    return;
                }
                try {
                    const res = await fetch(new URL("api.php?state=customer_me", window.location.href), {
                        headers: { 'X-Customer-Token': this.auth.token },
                        signal: AbortSignal.timeout(4000)
                    });
                    if (res.status === 401) {
                        localStorage.removeItem('vm_customer_token');
                        localStorage.removeItem('vm_customer_cache');
                        this.auth.user = null;
                        this.auth.token = null;
                        this.applyCustomerToDom();
                        document.dispatchEvent(new CustomEvent('vm:customer-logout'));
                        if ((window.location.hash || '').startsWith('#account')) this.route();
                        return;
                    }
                    if (!res.ok) throw new Error('boot failed: ' + res.status);
                    const data = await res.json();
                    if (data && data.ok && data.customer) {
                        this.auth.user = data.customer;
                        localStorage.setItem('vm_customer_cache', JSON.stringify(data.customer));
                        this.applyCustomerToDom();
                        document.dispatchEvent(new CustomEvent('vm:customer-login', { detail: data.customer }));
                    }
                } catch (e) { /* network failure */ }
            }

            async login(email, password) {
                const result = await postStore('customer_login', { email, password });
                if (result.ok && result.data.token && result.data.customer) {
                    this.auth.token = result.data.token;
                    this.auth.user = result.data.customer;
                    localStorage.setItem('vm_customer_token', result.data.token);
                    localStorage.setItem('vm_customer_cache', JSON.stringify(result.data.customer));
                    this.applyCustomerToDom();
                    document.dispatchEvent(new CustomEvent('vm:customer-login', { detail: result.data.customer }));
                    return { ok: true };
                }
                return { ok: false, error: result.error || 'Sign-in failed' };
            }

            async register(email, password, name) {
                const result = await postStore('customer_register', { email, password, name });
                if (result.ok && result.data.token && result.data.customer) {
                    this.auth.token = result.data.token;
                    this.auth.user = result.data.customer;
                    localStorage.setItem('vm_customer_token', result.data.token);
                    localStorage.setItem('vm_customer_cache', JSON.stringify(result.data.customer));
                    this.applyCustomerToDom();
                    document.dispatchEvent(new CustomEvent('vm:customer-login', { detail: result.data.customer }));
                    return { ok: true };
                }
                return { ok: false, error: result.error || 'Could not create account' };
            }

            async logout() {
                try { await postStore('customer_logout', {}); } catch {}
                this.auth.token = null;
                this.auth.user = null;
                localStorage.removeItem('vm_customer_token');
                localStorage.removeItem('vm_customer_cache');
                this.applyCustomerToDom();
                document.dispatchEvent(new CustomEvent('vm:customer-logout'));
            }

            initLayout() {
                const mobBtn = document.getElementById('mobile-menu-btn');
                const mobMenu = document.getElementById('mobile-menu');
                const mobClose = document.getElementById('mobile-close-btn');
                
                const toggle = () => mobMenu.classList.toggle('hidden');
                
                mobBtn.onclick = toggle;
                mobClose.onclick = toggle;
                document.querySelectorAll('.js-mobile-link').forEach(link => link.onclick = toggle);
            }

            showToast(msg) {
                const container = document.getElementById('toast-container');
                const toast = document.createElement('div');
                toast.className = 'bg-volume-rust text-volume-paper px-6 py-3 rounded-full text-[10px] uppercase font-bold shadow-2xl transition-all duration-300 transform translate-y-10 opacity-0';
                toast.textContent = msg;
                container.appendChild(toast);
                
                requestAnimationFrame(() => toast.classList.remove('translate-y-10', 'opacity-0'));
                setTimeout(() => {
                    toast.classList.add('translate-y-10', 'opacity-0');
                    setTimeout(() => toast.remove(), 300);
                }, 2500);
            }

            initRouter() {
                window.addEventListener('hashchange', () => this.route());
                this.route();
            }

            route() {
                const hash = window.location.hash.slice(1) || 'home';
                const parts = hash.split('/');
                const view = parts[0];
                const id = parts[1];

                this.root.classList.remove('fade-in');
                void this.root.offsetWidth;
                this.root.classList.add('fade-in');
                window.scrollTo(0, 0);

                switch(view) {
                    case 'shop': this.renderShop(id); break;
                    case 'product': this.renderProduct(id); break;
                    case 'cart': this.renderCart(); break;
                    case 'checkout': this.renderCheckout(); break;
                    case 'success': this.renderSuccess(); break;
                    case 'account': this.renderAccount(); break;
                    case 'about': this.renderTemplate('tpl-about'); break;
                    default: this.renderTemplate('tpl-home'); break;
                }
            }

            renderTemplate(templateId, callback = null) {
                const tpl = document.getElementById(templateId);
                if (!tpl) return;
                this.root.innerHTML = '';
                this.root.appendChild(tpl.content.cloneNode(true));
                if (callback) callback.call(this, this.root);
            }

            renderShop() {
                this.renderTemplate('tpl-shop', (view) => {
                    const grid = view.querySelector('.js-product-grid');
                    const prodTpl = document.getElementById('tpl-product-card');
                    
                    view.querySelector('.js-collection-count').textContent = `${DB.products.length} Iterations`;

                    DB.products.forEach(p => {
                        const card = prodTpl.content.cloneNode(true);
                        card.querySelector('.js-prod-link').href = `#product/${p.id}`;
                        card.querySelector('.js-title').textContent = p.title;
                        card.querySelector('.js-price').textContent = `${DB.currency}${p.price}`;
                        card.querySelector('.js-category').textContent = p.category;
                        card.querySelector('.js-img-placeholder').textContent = `[Specimen_${p.img}]`;
                        grid.appendChild(card);
                    });
                });
            }

            renderProduct(id) {
                const product = DB.products.find(p => p.id == id);
                if (!product) return window.location.hash = 'shop';

                this.renderTemplate('tpl-product', (view) => {
                    view.querySelector('.js-detail-title').textContent = product.title;
                    view.querySelector('.js-detail-price').textContent = `${DB.currency}${product.price}`;
                    view.querySelector('.js-detail-cat').textContent = `Volume_Index: ${product.id}`;
                    view.querySelector('.js-detail-desc').textContent = product.desc;
                    view.querySelector('.js-detail-img').textContent = `FILE://artifact/${product.img}.EXE`;

                    view.querySelector('.js-add-btn').onclick = () => {
                        this.cartAdd(product);
                        this.showToast(`Cached Artifact: ${product.title}`);
                    };
                });
            }

            renderCart() {
                if (this.cart.length === 0) {
                    this.renderTemplate('tpl-cart-empty', (emptyView) => {
                        this.renderTemplate('tpl-cart', (mainView) => {
                            mainView.querySelector('.js-cart-container').appendChild(emptyView);
                        });
                    });
                    return;
                }

                this.renderTemplate('tpl-cart', (mainView) => {
                    const container = mainView.querySelector('.js-cart-container');
                    const layoutTpl = document.getElementById('tpl-cart-layout').content.cloneNode(true);
                    const listContainer = layoutTpl.querySelector('.js-cart-items');
                    const itemTpl = document.getElementById('tpl-cart-item');
                    
                    let total = 0;

                    this.cart.forEach((item, index) => {
                        total += (item.price * item.qty);
                        const row = itemTpl.content.cloneNode(true);
                        
                        row.querySelector('.js-item-title').textContent = item.title;
                        row.querySelector('.js-item-price').textContent = `${DB.currency}${item.price}`;
                        row.querySelector('.js-item-qty').textContent = item.qty;
                        row.querySelector('.js-item-img').textContent = `Spec_${item.img}`;

                        row.querySelector('.js-qty-add').onclick = () => this.cartUpdate(index, 1);
                        row.querySelector('.js-qty-sub').onclick = () => this.cartUpdate(index, -1);
                        row.querySelector('.js-item-remove').onclick = () => {
                            this.cart.splice(index, 1);
                            this.cartSave();
                            this.renderCart();
                        };

                        listContainer.appendChild(row);
                    });

                    layoutTpl.querySelector('.js-cart-total').textContent = `${DB.currency}${total.toFixed(2)}`;
                    container.appendChild(layoutTpl);
                });
            }

            renderCheckout() {
                if(this.cart.length === 0) return window.location.hash = 'cart';
                this.renderTemplate('tpl-checkout', (view) => {
                    view.querySelector('.js-checkout-form').onsubmit = (e) => {
                        e.preventDefault();
                        this.cart = []; 
                        this.cartSave();
                        window.location.hash = 'success';
                    };
                });
            }

            renderSuccess() { this.renderTemplate('tpl-success'); }

            renderAccount() {
                this.renderTemplate('tpl-account', (view) => {
                    const showSection = (which) => {
                        view.querySelector('.acc-login').classList.toggle('hidden', which !== 'login');
                        view.querySelector('.acc-register').classList.toggle('hidden', which !== 'register');
                        view.querySelector('.acc-dashboard').classList.toggle('hidden', which !== 'dashboard');
                    };
                    const setError = (which, msg) => {
                        const el = view.querySelector('[data-err="' + which + '"]');
                        if (!el) return;
                        if (msg) { el.textContent = msg; el.classList.remove('hidden'); }
                        else { el.textContent = ''; el.classList.add('hidden'); }
                    };

                    if (this.isLoggedIn()) { showSection('dashboard'); this.renderDashboard(view); }
                    else { showSection('login'); }

                    view.querySelectorAll('[data-action="show-register"]').forEach(el => {
                        el.addEventListener('click', (e) => { e.preventDefault(); setError('login', ''); showSection('register'); });
                    });
                    view.querySelectorAll('[data-action="show-login"]').forEach(el => {
                        el.addEventListener('click', (e) => { e.preventDefault(); setError('register', ''); showSection('login'); });
                    });

                    const loginForm = view.querySelector('[data-form="login"]');
                    if (loginForm) {
                        loginForm.addEventListener('submit', async (e) => {
                            e.preventDefault();
                            setError('login', '');
                            const fd = new FormData(loginForm);
                            const result = await this.login(fd.get('email'), fd.get('password'));
                            if (result.ok) { showSection('dashboard'); this.renderDashboard(view); }
                            else setError('login', result.error);
                        });
                    }

                    const registerForm = view.querySelector('[data-form="register"]');
                    if (registerForm) {
                        registerForm.addEventListener('submit', async (e) => {
                            e.preventDefault();
                            setError('register', '');
                            const fd = new FormData(registerForm);
                            const result = await this.register(fd.get('email'), fd.get('password'), fd.get('name'));
                            if (result.ok) { showSection('dashboard'); this.renderDashboard(view); }
                            else setError('register', result.error);
                        });
                    }

                    const logoutBtn = view.querySelector('.js-logout-btn');
                    if (logoutBtn) {
                        logoutBtn.addEventListener('click', async () => {
                            await this.logout();
                            showSection('login');
                        });
                    }
                });
            }

            async renderDashboard(view) {
                const root = view || this.root;
                const user = this.auth.user || {};
                const nameEl = root.querySelector('.js-acc-name');
                const emailEl = root.querySelector('.js-acc-email');
                if (nameEl) nameEl.textContent = user.name || 'Friend';
                if (emailEl) emailEl.textContent = user.email || '';

                const ordersEl = root.querySelector('.js-orders-list');
                if (!ordersEl) return;
                ordersEl.innerHTML = '<p class="text-[10px] uppercase tracking-widest text-volume-slate py-4 text-center">Loading…</p>';

                let orders = [];
                try { orders = await fetchStore('customer_my_orders'); } catch { orders = []; }
                if (!Array.isArray(orders)) orders = [];
                ordersEl.innerHTML = '';
                if (orders.length === 0) {
                    ordersEl.innerHTML = '<p class="text-[10px] uppercase tracking-widest text-volume-slate py-4 text-center">No orders yet.</p>';
                    return;
                }
                orders.slice(0, 5).forEach(o => {
                    const row = document.createElement('div');
                    row.className = 'flex justify-between items-center py-3 border-b border-volume-charcoal/5 text-[10px] uppercase tracking-widest';
                    const id = o.id || '?';
                    const total = o.total_amount != null ? o.total_amount : (o.total || '?');
                    const status = (o.status || 'pending').toString();
                    const date = (o.created_at || '').toString().substring(0, 10);
                    row.innerHTML =
                        '<span class="text-volume-slate">#' + String(id).padStart(4, '0') + '</span>' +
                        '<span class="text-volume-slate">' + date + '</span>' +
                        '<span class="text-volume-charcoal">' + total + '</span>' +
                        '<span class="font-bold text-volume-charcoal">' + status + '</span>';
                    ordersEl.appendChild(row);
                });
            }

            cartAdd(product) {
                const existing = this.cart.find(item => item.id === product.id);
                if (existing) { existing.qty += 1; } 
                else { this.cart.push({ ...product, qty: 1 }); }
                this.cartSave();
            }

            cartUpdate(index, amount) {
                this.cart[index].qty += amount;
                if (this.cart[index].qty <= 0) this.cart.splice(index, 1);
                this.cartSave();
                this.renderCart(); 
            }

            cartSave() {
                localStorage.setItem('vol_cart', JSON.stringify(this.cart));
                this.updateCartCount();
            }

            updateCartCount() {
                const count = this.cart.reduce((sum, item) => sum + item.qty, 0);
                document.querySelectorAll('.js-cart-count').forEach(el => el.textContent = count);
            }
        }

        // --- 3. BOOTSTRAP ---
        /* ════════════════════
           CANONICAL DATA LAYER — fetchStore → api.php?state=X, getMockData fallback, MOCK shape
        ════════════════════ */
        const __CATS = [...new Set(DB.products.map(p => p.category).filter(Boolean))].map((name, i) => ({ id: i + 1, name }));
        const MOCK = {
            site: { name: "STREET", currency: DB.currency || "R", domain: window.location.hostname },
            categories: __CATS,
            products: DB.products.map((p, i) => ({
                ...p,
                id: p.id != null ? p.id : i + 1,
                name: p.name || p.title || "",
                image: p.image || p.img || "",
                description: p.description || p.desc || "",
                stock: p.stock != null ? p.stock : 12,
                category_id: (__CATS.find(c => c.name === p.category) || {}).id || null
            })),
            discounts: [
                { id: 1, code: "WELCOME10", percentage: 10, active: 1 },
                { id: 2, code: "DROP20", percentage: 20, active: 1 }
            ]
        };
        async function fetchStore(state, params = {}) {
            const url = new URL("api.php", window.location.href);
            url.searchParams.set("state", state);
            for (const [k, v] of Object.entries(params)) url.searchParams.set(k, v);
            const headers = {};
            const token = localStorage.getItem('vm_customer_token');
            if (token) headers['X-Customer-Token'] = token;
            try {
                const res = await fetch(url, { signal: AbortSignal.timeout(4000), headers });
                if (!res.ok) throw new Error(res.status);
                return await res.json();
            } catch {
                return getMockData(state, params);
            }
        }

        async function postStore(state, body = {}) {
            const url = new URL("api.php", window.location.href);
            url.searchParams.set("state", state);
            const headers = { 'Content-Type': 'application/json' };
            const token = localStorage.getItem('vm_customer_token');
            if (token) headers['X-Customer-Token'] = token;
            try {
                const res = await fetch(url, {
                    method: 'POST',
                    headers,
                    body: JSON.stringify(body),
                    signal: AbortSignal.timeout(8000)
                });
                const data = await res.json().catch(() => ({}));
                return { ok: res.ok && (data.ok !== false), status: res.status, data, error: data.error || null };
            } catch (e) {
                return { ok: false, status: 0, data: {}, error: e.message };
            }
        }
        function getMockData(state, params = {}) {
            switch (state) {
                case "site": return MOCK.site;
                case "products": return MOCK.products;
                case "product": return MOCK.products.find(p => p.id == params.id) || null;
                case "categories": return MOCK.categories;
                case "products_by_category": return MOCK.products.filter(p => p.category_id == params.category_id);
                case "search": {
                    const q = (params.q || "").toLowerCase();
                    return MOCK.products.filter(p => (p.name || "").toLowerCase().includes(q) || (p.description || "").toLowerCase().includes(q));
                }
                case "discounts": return MOCK.discounts;
                case "orders": return [];
                default: return [];
            }
        }

        document.addEventListener('DOMContentLoaded', async () => {
            try { const __d = await fetchStore('products'); DB.products = __d.map(p => ({ ...p, title: p.title || p.name || '', img: p.img || p.image || '', desc: p.desc || p.description || '', category: p.category || ((MOCK.categories.find(c => c.id == p.category_id) || {}).name) || '' })); } catch {}
            window.App = new StoreEngine();
        });
    </script>
</body>
</html>